{
  "installCommand": "npm install -g pnpm@11.1.1 && cd sdk && pnpm install --frozen-lockfile --ignore-scripts && pnpm build && cd ../poc && npm install --include=dev --ignore-scripts",
  "buildCommand": "cd poc && npm run build",
  "outputDirectory": "poc/dist"
}
